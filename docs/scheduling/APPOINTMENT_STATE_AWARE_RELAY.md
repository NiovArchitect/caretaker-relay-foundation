# Appointment State-Aware Relay

`NEXT_APPOINTMENT` and reminders exclude: cancelled, completed, missed, rescheduled (superseded).

Lifecycle states: proposed, requested, tentative, confirmed, cancelled, rescheduled, completed, missed.

Tests: `tests/unit/care/relay-appointment-state-matrix.test.ts`
